'''
HETLAND CHAPTER 3!!!!!!
String Formatting
   methods:
      center()
      ralign()
      strip()
      rstrip()
      format()
    f-string (string interpolation + formatting)

    f"{var:>20s}"
    f"{var:>5d}"
    f"{var:10.2f}
    alignment   < ^ >
    digits      how many
    decimals    how precise
    data type   s d f

String FINDING:
    if 'ab' in 'Crab':
    'Crab'.find('ab')
    Regular Expressions (RegEx) (CHAPTER 7)
        The ? matches zero or one of the preceding group.
        The * matches zero or more of the preceding group.
        The + matches one or more of the preceding group.
        The . matches any character, except newline characters.
        \d, \w, and \s match a digit, word, or space character, respectively.
        \D, \W, and \S match anything except a digit, word, or space character, respectively.
        [abc] matches any character between the brackets (such as a, b, or c).

Dictionaries:
    vs. List vs. Class
    key vs value
    Get + Add + Remove + Iterate
        # LIST
        for idx in range(0, len(lst)):
            val = lst[idx]

        # DICTIONARIES
        for key in dict:
            val = dict[key]

Classes & Objects;
    CONCEPTS & VOCAB
    HETLAND CHAPTER 7!!!!!!
    Inheritance, Encapsulation, Polymorphism
    Class vs. Object (Instance)
    Functions vs. Methods (State / self)

File i/o
    Open modes   r w a
    with / as  (why??????)
    stream i/o
    stdin r stream   stdout w/a stream (print)
    relative vs. abs paths!!!!!!!!
    piping   |    output | input
    output redirection > >>      > overwrite   >> append
'''